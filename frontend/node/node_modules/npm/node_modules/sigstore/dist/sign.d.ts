/// <reference types="node" />
import { SignerFunc } from 'sigstore/dist/types/signature';
import * as sigstore from 'sigstore/dist/types/sigstore';
import type { CA } from 'sigstore/dist/ca';
import type { Provider } from 'sigstore/dist/identity';
import type { TLog } from 'sigstore/dist/tlog';
import type { TSA } from 'sigstore/dist/tsa';
export interface SignOptions {
    ca: CA;
    tlog: TLog;
    tsa?: TSA;
    identityProviders: Provider[];
    tlogUpload?: boolean;
    signer?: SignerFunc;
}
export declare class Signer {
    private ca;
    private tlog;
    private tsa?;
    private tlogUpload;
    private signer;
    private identityProviders;
    constructor(options: SignOptions);
    signBlob(payload: Buffer): Promise<sigstore.Bundle>;
    signAttestation(payload: Buffer, payloadType: string): Promise<sigstore.Bundle>;
    private signWithEphemeralKey;
    private getIdentityToken;
}
